# set up 256 colors
term "xterm-256color"
attrcolor b ".I"

# Use UTF-8 like a human in 2014
utf8                    on

# Host, date, window list
hardstatus alwayslastline "%D %d-%M-%Y %c [ %w ]"

# for persistent ssh connections
setenv SSH_AUTH_SOCK $HOME/.ssh/ssh_auth_sock

